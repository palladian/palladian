package tud.iir.news;

import org.junit.Ignore;
import org.junit.Test;

public class FeedClassifierTest {

    @Test
    @Ignore
    public void testFeedClassification() {

        // http://beta.rottentomatoes.com/syndication/rss/upcoming.xml
        /*Assert.assertEquals(FeedClassifier.CLASS_ON_THE_FLY, FeedClassifier.classify("data/test/feeds/feed1.xml"));

        // http://29content6.com/nipponsport/NIPPONSPORTpodcast.xml
        Assert.assertEquals(FeedClassifier.CLASS_SPONTANUOUS, FeedClassifier.classify("data/test/feeds/feed2.xml"));

        // http://beta.rottentomatoes.com/syndication/rss/upcoming.xml (set back to older date)
        Assert.assertEquals(FeedClassifier.CLASS_CHUNKED, FeedClassifier.classify("data/test/feeds/feed3.xml"));

        // http://www.amazingwomenbbs.com/amazingwomen.rss
        Assert.assertEquals(FeedClassifier.CLASS_ZOMBIE, FeedClassifier.classify("data/test/feeds/feed4.xml"));

        // http://feeds.nydailynews.com/nydnrss/news
        Assert.assertEquals(FeedClassifier.CLASS_CONSTANT, FeedClassifier.classify("data/test/feeds/feed5.xml"));

        // http://feeds.gawker.com/lifehacker/full
        Assert.assertEquals(FeedClassifier.CLASS_SLICED, FeedClassifier.classify("data/test/feeds/feed6.xml"));

        // http://www.reddit.com/new/.rssl
        Assert.assertEquals(FeedClassifier.CLASS_CONSTANT, FeedClassifier.classify("data/test/feeds/feed7.xml"));*/
    }

}