package tud.iir.knowledge;

import org.junit.Test;

public class RegExpTest {
    @Test
    public void testRegExp() {

        /*
         * String text = " asdAprasd";
         * Pattern pattern;
         * Matcher matcher;
         * pattern = Pattern.compile(RegExp.MONTH_NAME_SHORT_ENG);
         * matcher = pattern.matcher(text);
         * matcher.find();
         * int start = matcher.start();
         * int end = matcher.end();
         * assertEquals("Apr", text.substring(start, end));
         * text = " asdAprSunasd";
         * pattern = Pattern.compile(RegExp.WEEKDAY_NAME_SHORT);
         * matcher = pattern.matcher(text);
         * matcher.find();
         * start = matcher.start();
         * end = matcher.end();
         * assertEquals("Sun", text.substring(start, end));
         * text = " asdAprSunas07d";
         * pattern = Pattern.compile(RegExp.DAY_OF_MONTH);
         * matcher = pattern.matcher(text);
         * matcher.find();
         * start = matcher.start();
         * end = matcher.end();
         * assertEquals("07", text.substring(start, end));
         * text = " asdAprSunas2010d";
         * pattern = Pattern.compile(RegExp.YEAR_SHORT_LONG);
         * matcher = pattern.matcher(text);
         * matcher.find();
         * start = matcher.start();
         * end = matcher.end();
         * assertEquals("2010", text.substring(start, end));
         * text = "asdTuesday, 02-Jul-10 33:07:49 GMT asd4fTuesday, 02-Jul-10 19:07:49 GMTasdr2";
         * pattern = Pattern.compile(RegExp.DATE_RFC_1036[0]);
         * matcher = pattern.matcher(text);
         * matcher.find();
         * start = matcher.start();
         * end = matcher.end();
         * assertEquals("Tuesday, 02-Jul-10 19:07:49 GMT", text.substring(start, end));
         */
    }
}
